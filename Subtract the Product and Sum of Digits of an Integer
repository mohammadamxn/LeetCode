class Solution:
    def subtractProductAndSum(self, n: int) -> int:
        n = str(n)
        s = 0
        p = 1
        for d in n:
            p *= int(d)
            s += int(d)
        return p - s
